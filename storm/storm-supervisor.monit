check process supervisor with pidfile /var/service/pids/supervisor.pid
    start program = "/etc/init.d/storm supervisor start"
    stop program = "/etc/init.d/storm supervisor stop"
    if cpu usage is greater than 60 percent for 2 cycles then alert
    if 2 restarts within 4 cycles then timeout
    alert premal@grepdata.com

